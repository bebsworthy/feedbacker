# NPM Configuration
# This file configures npm behavior for the project

# Use exact versions in package-lock.json
save-exact=false

# Always use package-lock.json
package-lock=true

# Audit packages on install (security check)
audit-level=moderate

# Don't fund on install (cleaner output)
fund=false

# Registry configuration
registry=https://registry.npmjs.org/

# Publishing configuration
access=public

# Security: require HTTPS for registry
strict-ssl=true

# 2FA Protection (when publishing manually)
# This ensures 2FA is required for publishing
# (automation tokens bypass this)
# Uncomment if you want to enforce 2FA for manual publishes:
# two-factor=true